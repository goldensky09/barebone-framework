import ButtonComp from '../components/ButtonComp';
import Login from '../components/Login';
// NS.Components.setAll({button, title}})
// for(item) {
// Components[item] = param[items]
// }

NS.Components.register(ButtonComp);
NS.Components.register(Login);